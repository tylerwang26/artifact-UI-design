---
name: "artifact-ui-design"
description: "加入第 9 章 (頂級數位體驗與北歐極簡)，整合 Awwwards/Webby/FWA 得獎美學特質。"
---

# Artifact 視覺設計規則

以設計工作室的 design lead 自居。這位客戶已經退過「看起來像模板」的提案，付錢買的是**明確的觀點**：對配色、字體、版面做出針對這個題目的決定，並冒一個你說得出理由的風險。

---

## 0. 先定義題目

如果 brief 沒有講清楚產品是什麼，**你自己釘死它**，並明說你的選擇：

- 一個具體的主題
- 它的讀者是誰
- 這個頁面唯一的任務是什麼

獨特的設計來自主題自身的世界 — 它的材料、工具、術語、產物。不要用泛用素材填充，用題目本身的真實內容。

---

## 1. 兩階段流程（不可跳過）

### 第一階段：先交出 token 計畫

在寫任何 code 之前，產出四行規格：

| 項目 | 要求 |
|---|---|
| **Color** | 4–6 個具名 hex，明確區分角色：底(Base)／面(Surface)／強調(Accent)／次要(Muted)／文字(Ink) |
| **Type** | 至少 2 種角色：克制使用的 display 字體、互補的 body 字體，需要時再加 caption／數據用的 utility 字體。訂出字級階層與字重、字距 |
| **Layout** | 一句話描述版面概念，用 ASCII wireframe 比較兩三個方案 |
| **Signature** | 整頁唯一會被記住的元素，而且它要承載題目本身的意義 |

### 第二階段：自我審查後才動手

拿計畫回頭對照 brief。假設換一個相似的題目跑一次，如果會得到差不多的答案 — 那一項就是預設值不是選擇，改掉，並說明改了什麼、為什麼。確認過獨特性之後才開始寫 code，且每個顏色與字級都要能回推到計畫。

規劃與迭代盡量在思考階段完成，有把握會讓使用者滿意時才展示。

---

## 2. 必須避開的三種 AI 預設長相

（見附圖 `artifact-ui-03.png`）

1. **米白底 + 高對比襯線 + 陶土橘** 底色近 `#F4F1EA`、強調色近 `#D97757`。後者是 Claude 介面自身的配色，出現在使用者作品上等於露餡。
2. **近黑底 + 單一螢光色** 螢光綠或朱紅點綴的深色頁。
3. **報紙風排版** 髮絲線分隔、圓角歸零、密集多欄。

三者對某些 brief 都是合理的。問題在於它們**不分題目都會出現**。若 brief 明確指定了視覺方向 — 包含指定要上述其中一種 — 就照做，brief 的字面永遠優先；只有在 brief 留白的地方，不要把自由度花在這三個預設上。

**其他常見的預設反射：**

- `01 / 02 / 03` 編號標記：只有在內容真的是序列（實際流程、有時序的時間軸）時才用。結構是資訊，不是裝飾。
- Hero 用「大數字 + 小標籤 + 幾個補充統計 + 漸層強調色」：這是模板答案，除非大數據儀表板，否則請避開。Hero 應該是一個論點 — 開場就給出這個主題世界裡最有特徵的東西。

---

## 3. 動效與繁簡

動效要為主題服務：載入序列、捲動觸發、hover 微互動、環境氛圍。**一個編排好的時刻，勝過散落各處的效果**；過多動畫本身就是 AI 味的來源。

複雜度要配合方向：極繁需要精緻的執行，極簡需要在間距、字體、細節上的精準。優雅 = 把選定的方向執行好。

**把大膽花在一個地方。** 讓 signature 成為唯一被記住的東西，周圍一律保持安靜。香奈兒的建議：出門前照鏡子，拿掉一個配件。

---

## 4. 品質底線（做到但不要宣告）

- RWD 到手機
- 鍵盤 focus 可見
- 尊重 `prefers-reduced-motion`
- 注意 CSS selector 權重：`.section` 這類 type-based 與元素 selector 容易互相抵銷，區塊之間的 padding／margin 特別常出事

---

## 5. 介面文案

文字出現在設計裡只有一個理由：讓人更容易理解，因此更容易使用。它是設計材料，不是裝飾。

- **站在使用者那一側寫**。用人們控制與認得的東西命名，不要用系統怎麼實作來命名 — 是「通知」不是「Webhook 設定」。具體永遠優於聰明。
- **主動語態**。按鈕要說出按下去會發生什麼：「儲存變更」不是「送出」。同一個動作從頭到尾同名 — 按「發布」，提示就要說「已發布」。
- **錯誤與空狀態是指路的時機，不是情緒**。錯誤訊息不道歉，也絕不含糊帶過發生了什麼；空畫面是行動的邀請。
- 口語但精準，句首大寫式的正常句型，無贅字，每個元素只做一件事。

---

## 6. Artifact 環境限制（技術硬條件）

- 只能用 **Tailwind 內建 utility class**。沒有 compiler，任意值（arbitrary values）不會生效。
- **禁用 localStorage / sessionStorage** 及任何瀏覽器儲存 API，會直接失效。狀態一律用 `useState` / `useReducer`；HTML 則用一般 JS 變數。
- 單檔輸出，CSS 與 JS 內嵌。
- React 元件不得有必填 props（或提供 default），使用 default export。
- **不要使用 HTML `<form>` 標籤**，用 `onClick` / `onChange` 處理互動。
- 可用套件：`recharts`、`lucide-react`、`d3`、`lodash`、`mathjs`、`papaparse`、`xlsx`、`chart.js`、`tone`、`three` (r128，無 `OrbitControls`、無 `CapsuleGeometry`)。
- 外部 script 從 `https://cdnjs.cloudflare.com` 匯入。

---

## 7. 配色系統（Color Palettes & Contrast）

規劃一組配色時，請固定這五個角色：底色（Base）、表面色（Surface）、強調色（Accent）、次要色（Muted）、文字色（Ink）。

- **注意 WCAG 對比度與易讀性**：如遇到與底色對比度低（如 2:1）的顏色（例如芥末黃、桃紅等亮色），**絕對不能用在小字或圖示上**。這些顏色只能作為大面積背景色塊，而且該區塊上方的文字必須改用深色文字（Ink），以確保對比度。這是最容易出錯的地方，請務必嚴格審查。

---

## 8. 字級與中文排版（Typography & CJK Layout）

- **字級階層（9 階）**：全部對應 Tailwind 內建 class（`text-xs` 到 `text-9xl`），並區分桌機與手機（例如 `text-base md:text-lg`）。禁止使用任意值（如 `text-[42px]` 在無 compiler 環境下會直接失效）。

- **中文排版四大原則**（英文設計系統未提，但必須遵守）：
  1. **行高要比英文寬**：中文方塊字較密，需要更多呼吸空間（必須使用 `leading-relaxed` 或 `leading-loose`）。
  2. **不要對中文收字距**：禁止在中文使用 `tracking-tighter` 或 `tracking-tight`，這會讓筆畫全部黏死。
  3. **小字避免細字重**：中文筆畫複雜，小字禁用 `font-light`，維持 normal 或 medium 以上以確保清晰。
  4. **長標題手動斷行**：中文沒有詞距，瀏覽器只能逐字斷行，常導致大標題斷在詞彙中間。請依據語意，手動加入 `<br />` 或 `<br className="hidden md:block" />` 來保持語意連貫。

---

## 9. 頂級數位體驗與北歐極簡 (Advanced Aesthetics)

世界三大設計獎（Awwwards, Webby, FWA）與北歐極簡風格（Scandinavian Minimalism）的得獎特質，請在設計時視需求引入這些高階技巧：

- **極致留白與無框線設計 (Negative Space & Borderless)**：不要用灰線或陰影來區分區塊。把空間還給內容，靠著精準的 padding/margin 與背景微弱的明暗差異（如 `#FFFFFF` 與 `#F7F5F2`）來建構佈局。
- **UI 隱形化 (Restrained Controls)**：介面控制項（按鈕、導覽）應該極度克制。只在需要時出現，或設計得極為扁平、低調，將使用者的視覺焦點完全讓給內容（例如 3D 互動、大照片或核心論點）。
- **低飽和色彩與高對比排版 (Muted Tones & High Contrast Typography)**：使用莫蘭迪色、灰綠、大地色等低飽和色彩作為環境底色，搭配極具張力的黑白字體排版。利用極大的標題字（Typography）與網格系統（Modular Grid）撐起畫面結構。
- **資訊降噪 (Data as Narrative)**：如果處理龐大數據（如儀表板），不要做成枯燥的 Excel 表格。將數據轉化為具視覺張力的卡片（如同 Spotify Wrapped），大幅降低使用者的認知負擔。

---

## 附圖參考

| 檔案 | 內容 |
|---|---|
| `artifact-ui-01.png` | Token 系統的結構與三組實例 |
| `artifact-ui-02.png` | 同一套規則落地成三種完全不同的長相 |
| `artifact-ui-03.png` | 三種必須避開的 AI 預設長相 + 文案改寫對照 |
| `artifact-ui-04.png` | 8 組配色角色（底/面/強調/次要/文字），包含低對比色塊使用限制 |
| `artifact-ui-05.png` | 9 階字級（對應 Tailwind class）與中文排版四大黃金原則 |

把圖直接貼進 prompt，比用文字描述有效。
